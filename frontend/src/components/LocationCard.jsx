import{useRef,useState}from"react";export default function VoiceRecorder({onTranscription}){const[r,setR]=useState(false),ref=useRef(null);function toggle(){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR){alert("Speech recognition is not supported. Type the report instead.");return}if(r){ref.current?.stop();setR(false);return}const s=new SR();s.lang="en-IN";s.onresult=e=>onTranscription(e.results[0][0].transcript);s.onend=()=>setR(false);s.start();ref.current=s;setR(true)}return <div><label>🎤 Voice Report</label><button type="button" className="btn secondary" onClick={toggle}>{r?"Stop":"Start Voice Input"}</button></div>}