# Backend starter
Python + FastAPI + PostgreSQL + SQLAlchemy.
Routes planned: POST /api/reports, GET /api/incidents, GET /api/resources, POST /api/allocations.
AI keys stay on backend, never in React.
