POST /api/reports: multipart fields description, voice_text, image, lat, lng.
GET /api/incidents: incidents with severity/location.
GET /api/resources: resources and status.
POST /api/allocations: selected incident/resource allocation.
